/*
    TODO:

        complete demo gen
        store resolver vals & run as final

        complete package.json
        write README.md

        handleTreeItems to strategy pattern
        improve error handling
        add unit tests

        ? access global function in async
*/

import generate from './tasks/index.js';

generate();
